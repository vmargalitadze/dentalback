# Server Configuration
# PORT=3000      # for local development
PORT=80          # for production (дентал-фэмили.рф) - requires root privileges
# HOST=0.0.0.0   # optional; default 0.0.0.0 (work on domain, not only localhost)

# Database Configuration (if you add a database later)
# DATABASE_URL=your_database_url_here

# Email Configuration (if you add email notifications later)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASS=your_password
